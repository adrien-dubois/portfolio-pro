import React from 'react'

const Card = (props) => {

    const {id, name, description, tags, demo, github} = props.data;

    return (
        <div>
            Work Data Cards
        </div>
    )
}

export default Card
